#!/usr/bin/env node

const path = require('path');

// Try to load a local jake
try {
  require(path.join(process.cwd(), 'node_modules', 'jake'));
} catch (e) {
  // If that fails, likely running globally
  require('../lib/jake');
}

const args = process.argv.slice(2);

jake.run.apply(jake, args);
